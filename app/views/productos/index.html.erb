<% if notice %>
  <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-6">
    <%= notice %>
  </div>
<% end %>

<% content_for :title, "Productos" %>

<div class="mb-8">
  <div class="flex justify-between items-center">
    <h1 class="text-3xl font-bold text-gray-900">🛍️ Catálogo de Productos</h1>
    <%= link_to new_producto_path, class: "bg-blue-600 hover:bg-blue-700 text-white font-medium py-2 px-4 rounded-lg shadow-md transition duration-300 ease-in-out transform hover:scale-105" do %>
      <span class="mr-2">➕</span> Nuevo Producto
    <% end %>
  </div>
  <p class="text-gray-600 mt-2">Gestiona tu inventario de productos</p>
</div>

<div id="productos" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
  <% if @productos.any? %>
    <% @productos.each do |producto| %>
      <div class="bg-white rounded-lg shadow-md hover:shadow-lg transition-shadow duration-300 overflow-hidden">
        <div class="p-6">
          <div class="flex justify-between items-start mb-4">
            <h3 class="text-xl font-semibold text-gray-900 truncate"><%= producto.nombre %></h3>
            <span class="<%= producto.stock > 0 ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800' %> text-xs font-medium px-2.5 py-0.5 rounded-full">
              <%= producto.stock > 0 ? 'En Stock' : 'Agotado' %>
            </span>
          </div>
          
          <p class="text-gray-600 text-sm mb-4 line-clamp-3"><%= truncate(producto.descripcion, length: 100) %></p>
          
          <div class="flex justify-between items-center mb-4">
            <div>
              <span class="text-2xl font-bold text-blue-600">$<%= number_with_precision(producto.precio, precision: 2) %></span>
            </div>
            <div class="text-right">
              <p class="text-sm text-gray-500">Stock</p>
              <p class="text-lg font-semibold text-gray-900"><%= producto.stock %></p>
            </div>
          </div>
          
          <div class="flex space-x-2">
            <%= link_to producto, class: "flex-1 bg-blue-600 hover:bg-blue-700 text-white text-center py-2 px-4 rounded-md text-sm font-medium transition duration-200" do %>
              👁️ Ver
            <% end %>
            <%= link_to edit_producto_path(producto), class: "flex-1 bg-yellow-500 hover:bg-yellow-600 text-white text-center py-2 px-4 rounded-md text-sm font-medium transition duration-200" do %>
              ✏️ Editar
            <% end %>
          </div>
        </div>
      </div>
    <% end %>
  <% else %>
    <div class="col-span-full text-center py-12">
      <div class="text-gray-400 mb-4">
        <svg class="mx-auto h-12 w-12" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4" />
        </svg>
      </div>
      <h3 class="text-lg font-medium text-gray-900 mb-2">No hay productos</h3>
      <p class="text-gray-500 mb-4">Comienza agregando tu primer producto</p>
      <%= link_to new_producto_path, class: "bg-blue-600 hover:bg-blue-700 text-white font-medium py-2 px-4 rounded-lg" do %>
        ➕ Agregar Primer Producto
      <% end %>
    </div>
  <% end %>
</div>
